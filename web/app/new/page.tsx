import { PageStub } from "@/components/page-stub";

export default function Page() {
  return <PageStub title="New Order" note="The order form lives here in the live tool — composed from shared field components on the same design system." />;
}
